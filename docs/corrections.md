# Corrections log

Every bug, wrong assumption or process failure found during the build, with what caught it
and what changed. Kept because the brief says to publish the failures, and because a log of
what went wrong is better evidence of engineering judgement than a claim that nothing did.

Entries are appended, never edited. Newest last.

| Date | Stage | What was wrong | How it was caught | What changed |
|---|---|---|---|---|
| 2026-09-02 | 1 | Dockerfile pinned a uv base-image tag that does not exist. | First `docker build`. | Copy the uv binary from its release image into `python:3.12-slim`, pinned to the local uv version. |
| 2026-09-03 | 1 | Assumed daily ZIPs would exhaust the GBP 1 budget alert within a month. | Computing the cost properly in `docs/cost.md`. | Estimate corrected; storage is the only growing line and is pennies. |
| 2026-09-03 | 1 | Misread a different day's file as a CDN inconsistency mid-deploy. | Checking the ledger row's `source_key`. | None in code. Noted here because a wrong alarm is still a mistake. |
| 2026-09-07 | 1 | Rewriting git history to fix commit authorship invalidated the commit SHA the Cloud Run image was tagged with. | Reviewing what else referenced the old SHAs. | Rebuilt and re-applied with the new SHA. Lesson: history rewrites break anything keyed on SHAs; do them before any deploy or tag. |
| 2026-09-07 | 1 | CI ran every job twice per push because the workflow triggered on both `push` and `pull_request`. | Reading the checks tab on PR #1. | `push` trigger scoped to `main`. |
| 2026-09-07 | 2 | Hidden-fact detection used Python `id()` of lxml proxy objects, which are recycled, so the count varied between test runs. | A test that passed once and failed on the next run. | Ancestor walk instead of identity set. Expected count re-derived independently by regex over the raw file. Suite now run three times before any PR. |
| 2026-09-07 | 2 | A test assumed `core:` and `frs-core:` prefixes resolve to different namespace URIs. They resolve to the same one. | The test failing against real fixtures. | Test corrected to assert the actual, stronger property: different prefixes, same identity. |
| 2026-09-07 | 2 | The first dbt agreement test required every occurrence of a fact to carry identical text. 487 groups failed: non-numeric dates appear as `2025-12-31` in the hidden block and `31 December 2025` on the page, the same fact rendered two ways. An earlier integrity query had only checked numeric facts, so the claim "no group disagrees" was narrower than it sounded. | `dbt test` against real data. | Agreement is a numeric property; `stg_facts` keeps the hidden form as `text` and the printed form as `display_text`, and the test covers numeric facts only. |
| 2026-09-07 | 2 | Two row-size constants in the cost script were typed as estimates but commented as measured. | Re-reading the diff before commit. | Replaced with values measured from BigQuery (`LENGTH(TO_JSON_STRING(row))`) and the query recorded in the comment. |
| 2026-09-07 | 2 | `stg_facts` picked `text` and `value` with ANY_VALUE, which is arbitrary among ties, so the evaluation answer key could read differently on successive queries. | Pre-PR code-review pass. | Every pick is an ordered ARRAY_AGG. |
| 2026-09-07 | 2 | The agreement test ignored nil occurrences (COUNT DISTINCT drops NULLs), so a nil plus a valued occurrence passed as consistent; a second test duplicated it. | Pre-PR code-review pass. | Nil counts as a distinct value; the test warns instead of failing the build and `stg_facts.inconsistent` carries the flag forward. |
| 2026-09-07 | 2 | The README said dbt never creates a dataset, but nothing enforced it: the dataset name was assembled in dbt independently of Terraform, and dbt's default behaviour is to create missing datasets. | Pre-PR code-review pass. | `create_schema` is overridden to fail; verified that a wrong dataset name creates nothing. |
| 2026-09-07 | 2 | dbt's telemetry cookie was committed, telemetry was on, and `.gitignore` ignored every `logs/` directory in the repo rather than dbt's. | Pre-PR code-review pass. | File removed and ignored; telemetry off; pattern anchored. |
| 2026-09-07 | 2 | No dbt test ran in CI; `dbt parse` checks YAML and Jinja only, so a renamed column would have passed. | Pre-PR code-review pass, against the stage-end checklist. | CI authenticates through Workload Identity Federation and runs `dbt build` in a Terraform-owned CI dataset. |
| 2026-09-07 | 2 | A dataset grant to the Cloud Run service account was added for a dbt writer that does not exist. | Pre-PR code-review pass. | Removed. The CI identity has its own least-privilege grants. |
| 2026-09-08 | 3 | Billing labels included the model id, which contains dots; GCP label values allow only lowercase letters, digits, underscore and dash. Every real call would have failed with 400 and quarantined the whole batch. | Pre-PR code-review pass. | Label values are sanitised, with a test on the rule. Verified live. |
| 2026-09-08 | 3 | The number normaliser silently dropped signs on `--5` and `(-5)` and accepted `1 2 3`. | Pre-PR code-review pass, executed. | Strict grammar: one sign, brackets or dash but not both, thousands groups of exactly three digits. Anything else is a quarantine row. |
| 2026-09-08 | 3 | Only API errors were retried; transport errors escaped the model boundary with no quarantine reason. | Pre-PR code-review pass. | Transport errors retry like a 5xx; anything else is wrapped so it always has a reason. |
| 2026-09-08 | 3 | A truncated response (MAX_TOKENS) would have been misfiled as a schema failure. | Pre-PR code-review pass. | Any finish reason other than STOP raises TruncatedResponseError before validation. |
| 2026-09-08 | 3 | Cost per filing was estimated at roughly a quarter of the measured figure: the response schema and the evidence-quoted JSON output cost far more tokens than the filing text. | First live call through the real client. | Measured tokens replace the estimate; docs/cost.md will carry the measured per-1,000 figure once the batch runs. |
| 2026-09-08 | 3 | The extract job wrote both a JSON answer and its flattened values; a crash between the two loads could pair values from one attempt with JSON from another. | Pre-PR code-review pass. | The job writes the validated JSON only; dbt derives the flattened values. One source of truth. |
| 2026-09-08 | 3 | A resumed batch re-called the model for every pinned document, paying twice for answers already stored. | Pre-PR code-review pass. | A resume processes only pinned documents with no row yet; loads are keyed on batch and attempt. |
| 2026-09-08 | 3 | Rows quarantined during a resume were silently dropped, because the batch-level dedupe key had already been consumed by the crashed attempt. | A new test for a vanished document, written for the review's `MissingDocument` finding. | Load keys include the attempt id. |
| 2026-09-08 | 3 | A model id absent from the price table was recorded as costing zero. | Pre-PR code-review pass. | Unpriced real models are refused before any batch is pinned. |
| 2026-09-08 | 3 | The production CLI imported a helper from the tests package, which is not in the container image; `--fake` would have crashed on Cloud Run. | Pre-PR code-review pass. | The sample answer lives in the model module. |
| 2026-09-08 | 3 | The dbt quarantine test only accepted stage `parse`; the first extract batch would have turned CI red. | Pre-PR code-review pass. | Accepted values widened; model and prompt id exposed in the staging view. |
| 2026-09-08 | 3 | A scheduler grant was added to the extract job that no schedule uses, on a job whose every run spends money. | Pre-PR code-review pass. | Removed. |
| 2026-09-08 | 3 | The container image did not include the prompts directory; the first cloud execution of the extract job failed before any call. The review had caught the same class of bug for a test import an hour earlier and I did not generalise it. | First Cloud Run execution. | Dockerfile copies prompts; CI now runs each entrypoint inside the built image, so a file missing from the image fails the build. |
| 2026-09-08 | 3 | The backfill DAG deleted quarantine rows, which erased the history of what failed, and for the parse stage was a silent no-op because the scheduled selection skips sources already parsed at the current cap. | Pre-PR code-review pass. | Backfills stamp `released_at` instead of deleting; released rows stop counting as processed and go first in both jobs' selection; the parse rerun is one execution per affected source with `--source-key`. |
| 2026-09-08 | 3 | The BigQuery operator's default job id needs a logical date, which manual DAG runs in Airflow 3 do not have; the release task would have raised before submitting anything. | Pre-PR code-review pass, verified in the image. | Explicit job id. |
| 2026-09-08 | 3 | `make airflow-trigger` could not pass JSON parameters: the unquoted variable split the JSON into words. | Pre-PR code-review pass, verified. | A dedicated `CONF` variable, quoted in the recipe. |
| 2026-09-08 | 3 | The compose file mounted the whole gcloud directory, including the login refresh token, and bound the UI to every interface. | Pre-PR code-review pass. | Only the ADC file is mounted; the UI binds to localhost. |
| 2026-09-08 | 3 | The DAG files were written against the Airflow 2 API in three places (`get_dag`, `params[...].value`, a scheduler config key) although the image is Airflow 3. | The in-container integrity test and a deprecation warning. | Corrected. The DAG files are the one Python directory not under Pyright strict, because Airflow is not a project dependency; the in-container integrity test is their gate. |
| 2026-09-08 | 3 | Even with an explicit job id, the provider's BigQuery operator reads the run's logical date in `execute`, so every manual backfill run failed. The review had predicted the default-job-id case; the operator is unusable for logical-date-less runs altogether. | Triggering the DAG for real. | The release step submits its query through the BigQuery hook in a task. |
| 2026-09-08 | 3 | The BigQuery hook defaults to legacy SQL, which rejects backticked identifiers; the parse-branch sources query failed with an "invalid project id" message that pointed the wrong way. | Triggering the DAG for real. | Standard SQL set explicitly on the hook. |
| 2026-09-08 | 3 | The flattened-values dbt model referenced a Jinja parent-loop attribute that does not exist. | `dbt build` against BigQuery before commit. | One flat loop over field, period combinations. |
| 2026-09-08 | 4 | A reparse with an unchanged parser version reused the old batch id, so its loads were treated as already done and nothing was rewritten. | The reparse test, before commit. | A reparse salts its batch id with the run id; it is a new batch by definition. |
| 2026-09-08 | 4 | After the first cloud reparse, the documents-to-ledger reconciliation doubled: the ledger rightly keeps superseded batches as history, but the test summed them. | `dbt build` after the reparse. | Runs record whether they were a reparse; reconciliation counts only runs since the last reparse of each source. |
| 2026-09-08 | 4 | Prompt v1 said figures in brackets are negative. Accounts print creditors in brackets as presentation, and the taxonomy stores them as positive amounts, so every creditors extraction was sign-flipped: recall 54.5 percent on Flash-Lite and 13.2 percent on Flash, with no other error type present. | The evaluation harness, first run. | Prompt v2 states the taxonomy's sign convention; the harness measures the change. |
| 2026-09-08 | 4 | Measured effect of prompt v2 on Flash-Lite, same 300 filings: creditors recall 54.5 to 97.4 percent, overall recall 93.0 to 94.8, precision 93.9 to 95.6. Equity and net assets unchanged within noise; their remaining sign flips are tags filed without a sign attribute, which the harness cannot adjudicate and counts against the model. | The evaluation harness. | v2 is the default prompt from this point. |
| 2026-09-08 | 4 | A Flash variant with the thinking budget set to zero was run to price the cost gap. The model ignored the setting: 2,643 mean thinking tokens against 2,330 with the default, costing more and running slower. The experiment is a null result and the README says so. | The evaluation harness and the token counts on the rows. | Rows stay under the variant label with a note; a thinking-level control is a follow-up, not re-run now because the Stage 4 budget is spent. |
| 2026-09-08 | 4 | Stage 4 model spend came to about USD 6.4 against the USD 5 approved, because the no-thinking run was a null result that still cost full price. Flash was therefore not rerun on prompt v2, so the model comparison is fair only under v1 and the creditors row for Flash reflects the prompt defect. | Adding up the extraction rows. | Stated in the README; no further spend without approval. |
| 2026-09-08 | 5 | Event publishing logged with a keyword named `event`, which structlog reserves for the message itself; every ingest success path raised. | The existing ingest tests, on the first run after wiring. | Keyword renamed. |
| 2026-09-08 | 5 | Publishing sat inside the jobs' success path, so a Pub/Sub failure after the succeeded ledger row would have recorded a second, failed row and exited non-zero, and for extract mis-stated real spend. | Pre-PR code-review pass. | Publishing after success is best-effort through one helper that logs and never raises; the ledger is the source of truth and events are replayable from it. |
| 2026-09-08 | 5 | The dispatcher started a job on every delivery, so a redelivered event seconds after the first would have started a concurrent execution of the same pinned batch, double-spending on extraction. | Pre-PR code-review pass. | The dispatcher serialises executions per job: a busy job answers 503 and Pub/Sub retries with backoff. |
| 2026-09-08 | 5 | The extracted event crammed every source key of the batch into one attribute, which can exceed Pub/Sub's 1,024-byte attribute limit. | Pre-PR code-review pass. | Extracted events identify themselves by batch, model and prompt; source_key is optional for them and validated as required for the others. |
| 2026-09-08 | 5 | Tracing shipped without the Cloud Trace agent role on the jobs' service account; log correlation worked, span export was denied. | The first traced execution's logs. | Role granted in Terraform. |
| 2026-09-08 | 5 | The dead-letter purge task pulled with return-immediately semantics, which Pub/Sub may answer empty while messages are waiting; the DAG reported success and left the poison message in place. | Live run of the backfill DAG. | Blocking pull; the task loops until a pull is genuinely empty. |
| 2026-09-08 | 5 | The local Airflow metadata database is ephemeral, so every restart is a fresh install in which DAGs are paused by default; a triggered run sat queued for six minutes and the check read as a failure of the purge. | Live re-run of the backfill DAG. | DAGs are unpaused at creation in the compose file. |
| 2026-09-08 | 5 | Each live run of the backfill DAG also executed its extract branch with a one-document cap, adding filings to the scored sample; the README results block went stale and CI's results check failed the closing PR. | CI, on the closing PR. | Block regenerated. The check is doing exactly what it is for. |
| 2026-09-08 | 6 | The index job sent 32 chunks per embedding request. `gemini-embedding-2` treats a list of contents as one multimodal input and returns a single vector; the older `gemini-embedding-001` batches. | The length guard in the embedder, on the first cloud run ("embedded 1 of 3 texts"). The fake embedder cannot reproduce a provider quirk. | One text per request on a thread pool; the embedder asserts one vector of the configured dimensions per text. |
| 2026-09-08 | 6 | The first full index run hit a Vertex 429 after 206 seconds and had no retry; the extract job retries on 429 and 5xx but the embedder did not copy that. Worse, the chunks were only loaded at the end of the batch, so every embedding already paid for was thrown away, and the failed ledger row recorded zero tokens and zero cost. | The failed ledger row; the money was gone but the row said 0. | Embedder backs off and retries on 429 and 5xx. Chunks are loaded every 100 documents, each part keyed on batch, attempt and part, so a resume pays only for what was never loaded. A failed row records the counts up to the failure. Tests for both. |
| 2026-09-08 | 6 | A `git add -A` swept the binary Terraform plan file into a commit. Plan files can carry variable values and resource attributes and never belong in a repository. | `git diff --stat` before the next commit showed the plan file as modified. | The two unpushed commits were rewritten without the file; `tfplan` is ignored. Stage the paths you mean, not `-A`. |
| 2026-09-08 | 6 | The extraction tool's SQL used `@model IS NULL OR v.model = @model` and the helper dropped None parameters, so BigQuery rejected the query for naming a parameter it never received. A NULL-valued parameter is not sent either. | Driving the real server over stdio. The in-memory tests use a fake data layer and never ran the SQL. | Optional filters are appended to the SQL only when given. |
| 2026-09-08 | 6 | Joining extraction values to verdicts on document, model, prompt and field doubled every row: verdicts are per period too. | The same stdio run returned 36 rows for an 18-field extraction. | Period added to the join. |
| 2026-09-08 | 6 | The citation pattern accepted only eight-digit company numbers. An answer that cited a Scottish company (`SC041563`) listed two of its three citations; the third was neither resolved nor reported as unsupported. | Watching the README recording. The unit tests used numeric ids only. | Pattern accepts eight alphanumeric characters; test covers SC and NI prefixes. |
| 2026-09-08 | 6 | The vhs tape typed a pound sign into the shell, which arrived as a hash and commented out the whole command; the first recording showed an empty prompt where the answer should be. | Looking at the frames before committing the GIF. | ASCII only in the tape. |
